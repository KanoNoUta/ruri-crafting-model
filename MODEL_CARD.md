# RURI-Craft G1

| 字段 | 值 |
| --- | --- |
| 模型编号 | `RURI-Craft-G1` |
| 发布版本 | `1.0.0-beta.1` |
| 训练完成日期 | 2026-09-08 |
| 模型种类 | 小型 MLP 策略/价值/剩余次数模型；不是语言模型 |
| 结构 | 两个 512/256/128 网络，SiLU、LayerNorm、dropout 0.02 |
| 参数 | 451,404 |
| 路由 | 第 20 维 `craftState.isExpert`，索引从 0 开始 |
| 特征 / 动作 | 106 / 36；特征编码器 v2 |
| ONNX | FP32，opset 17，动态 batch |
| 许可 | Apache-2.0，适用于本次公开代码说明和模型权重 |
| 配方数值目录来源版本 | `2026.08.05.0000.0000`；不是对未来游戏版本的兼容承诺 |
| 训练数据 | 452,064 条去重教师状态，分区 362,342 / 45,272 / 44,450 |

## 输入与输出

输入包含角色的最终制作力、加工力、CP、等级、职业；配方难度、耐久、进度、品质、
初始品质；当前 CP、品质、进度、耐久、球色、技能效果和上一技能；目标与合法技能掩码。
不依靠 recipeId 或 itemId 作为模型特征，不从 teacher.status 决定推理分支。

`specialist` 是角色专家身份，`isExpert` 是专家配方，工具好球加成另用
`hasEnhancedGoodCondition`。食物、药水、装备与专家属性加成应已经体现在最终人物数值中。

输出为 `policy_logits`、`value`、`remaining_steps`。`remaining_steps` 的教师目标是
实际剩余技能调用数，包含不会推进通常步数计数器的技能调用。
`value` 是教师指标回归值，目前不能作为任意状态下已校准的制作成功率。

## 训练方法

G1 普通分支来自 normalized-cosine 第 179 轮，专家分支来自 warm-cosine 第 94 轮。
训练使用 AdamW、均匀动作交叉熵、专家状态三倍多任务权重、预热与余弦学习率。
标准化只拟合训练分区；分支选择用验证结果。历史留出集曾用于诊断单网络回退，
所以最终 92.01% 技能一致率是历史复用评估，不是新的独立盲测。

数据指纹：`e30bb5d2275672f08d4123f4a8d993efe0658942e89e80d2eeb577d82105f71d`。
训练代码及结构已公开，完整教师数据暂未发布；新留存补算数据不属于 G1。
模型仓库的 `modelmaster.json` 记录公开框架的固定 commit 和下载文件哈希。

## 已知限制

- 普通、严格专家、宇宙概率标签的覆盖与难度差异很大；部分技能正例稀少或为零。
- 严格/概率教师标签的语义不同，概率标签从未升级为成功保证。
- 合法掩码只能限制当前动作合法，不能保证整条流程完成或达到 HQ。
- 从未知失败状态的 value 不能推出可靠成功率；潜在校准问题尚未解决。
- 当前 ONNX 的 Where 会计算两个网络，因此推理成本高于旧单网络。
- 本次小测没有证明实际技能次数减少、全局最短路径或对上游 Raphael 的准确率优势。
- 宇宙高难、临时任务技能和未知条件模式需要严格遵守规则目录支持范围。
- 尚未在插件里验证在线推理、兼容更新或实际游戏制作流程。

## 发布权重的用途

`ruri-craft-g1.weights.pt` 包含精简的结构配置与张量，可使用
`torch.load(..., weights_only=True)` 加载，再由框架 `model_from_checkpoint` 恢复。
它适合推理与自定义微调初始化，不携带原优化器，不能直接当作原训练任务的断点恢复。
ONNX 文件与最终训练输出完全相同，SHA256：
`eac1b6dbe7f2963e57910200ae6692f1f969e04e9e1caf878195e74a472a02d3`。

## 使用建议

将 G1 作为候选动作或搜索排序来源，配合独立模拟与现有求解器。
升级前核对契约版本、技能顺序、规则和文件哈希，保留已验证的旧版。
本项目是独立社区研究，与 Square Enix 无从属关系。
